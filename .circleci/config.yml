version: 2.0
jobs:
  build:
    machine:
      image: circleci/classic:latest
    branches:
          only:
            - cicd
    steps:
      - run:
          name: Testing
          command: |
            sudo apt-get update
      - checkout
      
